import { Text, View } from "react-native";
import TxtComponent from "./TxtComponent";

export default function FlatComponent({
  // Props
  texto,
  styleTxt,
}) {
  return; // Props do TxtComponent para FlatList
  <TxtComponent texto={texto} styleTxt={styleTxt} />;
}
